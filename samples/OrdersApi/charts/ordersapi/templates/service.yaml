apiVersion: v1
kind: Service
metadata:
  name: {{ template "ordersapi.fullname" . }}
  labels:
    app: {{ template "ordersapi.name" . }}
    chart: {{ template "ordersapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "ordersapi.name" . }}
    release: {{ .Release.Name }}

apiVersion: v1
kind: Service
metadata:
  name: {{ template "frontendweb.fullname" . }}
  labels:
    app: {{ template "frontendweb.name" . }}
    chart: {{ template "frontendweb.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "frontendweb.name" . }}
    release: {{ .Release.Name }}
